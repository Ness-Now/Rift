# Shared Types

This package holds frontend-facing contracts that should remain small, explicit, and easy to mirror on the backend.

For now it only contains minimal system response shapes. As the product grows, shared contracts should stay focused on stable API boundaries rather than internal implementation details.
