# Contracts

Use this folder for future JSON Schema, OpenAPI extracts, or other machine-readable contract artifacts that need to be shared across applications.

The intent is to keep contract definitions close to the TypeScript package without forcing every internal shape into the shared surface area.
