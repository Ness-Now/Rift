# Prompts Package

This package stores versioned prompt assets separately from application code.

Each prompt family should have a stable directory, semantic version folders, metadata, and any output contract notes needed for deterministic backend orchestration.
