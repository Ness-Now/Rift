# Migrations Placeholder

Database migrations are intentionally deferred until the normalized schema is agreed and the API starts persisting data.

When we begin database work, this directory should hold the migration tool configuration and generated revisions.
